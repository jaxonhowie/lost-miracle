<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lostmiracle.module.admin.mapper.GmAccountMapper">

    <resultMap id="gmAccountResultMap" type="com.lostmiracle.module.admin.entity.GmAccountEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO gm_account (username, password_hash, role, status)
        VALUES (#{username}, #{passwordHash}, #{role}, #{status})
    </insert>

    <select id="selectByUsername" resultMap="gmAccountResultMap">
        SELECT * FROM gm_account WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="gmAccountResultMap">
        SELECT * FROM gm_account WHERE id = #{id}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM gm_account
    </select>

</mapper>
