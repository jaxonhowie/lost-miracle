<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lostmiracle.module.user.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.lostmiracle.module.user.entity.UserEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password_hash, status)
        VALUES (#{username}, #{passwordHash}, #{status})
    </insert>

    <select id="selectByUsername" resultMap="userResultMap">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <select id="countByUsername" resultType="long">
        SELECT COUNT(*) FROM `user` WHERE username = #{username}
    </select>

</mapper>
