<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lostmiracle.module.admin.mapper.GmAuditLogMapper">

    <resultMap id="gmAuditLogResultMap" type="com.lostmiracle.module.admin.entity.GmAuditLogEntity">
        <id property="id" column="id"/>
        <result property="gmAccountId" column="gm_account_id"/>
        <result property="action" column="action"/>
        <result property="targetType" column="target_type"/>
        <result property="targetId" column="target_id"/>
        <result property="detailJson" column="detail_json"/>
        <result property="ip" column="ip"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO gm_audit_log (gm_account_id, action, target_type, target_id, detail_json, ip)
        VALUES (#{gmAccountId}, #{action}, #{targetType}, #{targetId}, #{detailJson}, #{ip})
    </insert>

    <select id="selectRecent" resultMap="gmAuditLogResultMap">
        SELECT * FROM gm_audit_log
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>
